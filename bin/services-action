#!/bin/bash
#
action="$1"
[ "${action}" ] || action=status

for script in nginx comfyui lmstudio ollama openwebui openterminal vllm
do
  [ -f /lib/systemd/system/${script}.service ] || continue
  sudo systemctl ${action} ${script}
done
